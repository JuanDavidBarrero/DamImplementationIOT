import { DoorStatusPipe } from './door-status.pipe';

describe('DoorStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DoorStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
